ifndef::imagesdir[:imagesdir: ../images]

[[section-building-block-view]]
== Building Block View

Die Bausteinsicht zeigt die statische Struktur des Gesamtsystems auf drei Detaillierungsebenen: Gesamtsystem (Level 1), Subsysteme (Level 2) und Detailkomponenten (Level 3).

=== Whitebox Overall System (Level 1)

Das System besteht aus dem **Python Control Layer** (Steuerung, Validierung, API) und dem **Unity Simulation Layer** (3D-Visualisierung, Kinematik, Physik). Der physische **MyPalletizer 260 M5** ist als externes Zielsystem angebunden.

image::05_building_blocks_l1.png[align=center, width=70%]

[cols="1,3", options="header"]
|===
| Baustein | Verantwortung und Schnittstellen

| **Python Control Layer**
| Öffentliche API für Benutzer; Validierung und Begrenzung von Steuerwerten; modusspezifische Weiterleitung an Simulation (UDP) und/oder Hardware (Serial).

| **Unity Simulation Layer**
| Empfang von UDP-Nachrichten; 3D-Visualisierung des Roboterarms und der Werkzeuge; physikalische Gelenkbewegung via `ArticulationBody`; Verwaltung virtueller Objekte.

| **MyPalletizer 260 M5**
| Physische Hardware; Ausführung von Bewegungsbefehlen und Werkzeugaktionen über interne Servomotoren und Relais.

| **Benutzer / Entwickler**
| Erstellt Steuerungsskripte unter ausschliesslicher Verwendung der Klassen `Robot` und `World`.
|===

=== Level 2: Subsysteme

==== Whitebox Python Control Layer

Der Python Control Layer gliedert sich in benutzernahe Fassadenklassen, den zentralen Controller sowie Hilfsmodule für Protokollerstellung, vordefinierte Presets, Datenklassen und Ausnahmen.

image::05_python_l2.png[align=center, width=80%]

[cols="1,3", options="header"]
|===
| Baustein | Verantwortung und Aufgaben

| **`Robot`**
| High-Level-Fassade für die Robotersteuerung (`send_angles`, `sync_move_joints`, `set_color`, `set_end_effector`, `set_gripper_state`, `pump_on`, `pump_off`). Unterstützt das Python Context-Manager-Protokoll (`__enter__`, `__exit__`).

| **`MyPalletizerController`**
| Zentraler Dispatcher und Treiber. Führt Software-Clamping für Winkel, Geschwindigkeit und RGB durch; verwaltet den lokalen Zustandsspeicher `_sim_angles`; hält den UDP-Socket für Port `5005`; kapselt die `MyPalletizer260`-Hardwareinstanz.

| **`World`**
| High-Level-Fassade für die Umgebungsverwaltung (`spawn`, `delete`, `clear`, `load_preset`). Sendet über einen separaten UDP-Socket auf Port `5006`.

| **`protocol`**
| Zentrale Factory zur Konstruktion einheitlicher JSON-Nachrichten mit Versionsfeld (`"v": 1`) und Typkennung (`"type"`).

| **`presets`**
| Katalog vorgefertigter Szenarien (`empty`, `three_blocks`, `pick_and_place_basic`, `sorting_task`).

| **`objects`**
| Datenklassen für 3D-Körper: `WorldObject` (Basisklasse) sowie `Box`, `Sphere` und `Cylinder`.

| **`errors`**
| Benutzerdefinierte Ausnahmen (`InvalidConfigError`, `RobotConnectionError`, `WorldConnectionError`).

| **`pymycobot`**
| Externe Herstellerbibliothek zur seriellen Kommunikation mit dem MyPalletizer 260 M5.
|===

==== Whitebox Unity Simulation Layer

Die Simulationsarchitektur in Unity trennt strikt zwischen Roboter-Subsystem (Port `5005`) und Welt-Subsystem (Port `5006`).

image::05_unity_l2.png[align=center, width=85%]

[cols="1,3", options="header"]
|===
| Baustein | Verantwortung und Aufgaben

| **`UnityUdpBridge`**
| Empfängt UDP-Datagramme auf Port `5005` in einem Hintergrund-Thread und reiht sie threadsicher in eine Queue ein. Der `CommandRunner` leitet die Befehle im Hauptthread weiter.

| **`MyPalletizerArticulationAdapter`**
| Koordiniert die vier Gelenkaktuatoren `j1` bis `j4`. Bietet `MoveJointsAsync` (asynchron) und `MoveJointsSync` (synchron via Coroutine).

| **`ArticulationJointActuator`**
| Steuert den `ArticulationBody` eines einzelnen Gelenks über dessen `xDrive` und führt die geschwindigkeitsabhängige Winkelinterpolation im `FixedUpdate` aus.

| **`GripperDownConstraint`**
| Hält den Werkzeugkopf im `LateUpdate` vertikal nach unten ausgerichtet (Kompensation der Palettiermechanik).

| **`RobotToolManager`**
| Schaltet die Sichtbarkeit der Endeffektor-Modelle (`gripperTool` / `pumpTool`) und leitet Aktionen an den aktiven Werkzeugcontroller weiter.

| **`SimpleGripperController`**
| Führt mechanische Greifvorgänge aus; erfasst `GrabbableObject`-Objekte via `Physics.OverlapSphere` und bindet sie kinematisch an den Greifer.

| **`SuctionPumpController`**
| Steuert die Vakuumpumpe; koppelt das nächstgelegene `GrabbableObject` bei aktivem Unterdruck an den Saugkopf.

| **`RobotLEDController`**
| Steuert die RGB- und Emissionsdarstellung der Basis-LEDs des simulierten M5-Moduls.

| **`WorldUdpBridge`**
| Empfängt UDP-Datagramme auf Port `5006` und übergibt sie im `Update`-Frame an den `WorldSpawnManager`.

| **`WorldSpawnManager`**
| Instanziiert Prefabs (`boxPrefab`, `spherePrefab`, `cylinderPrefab`), konfiguriert `Rigidbody` und `GrabbableObject`, weist Farben zu und verwaltet aktive Objekte (`_spawnedObjects`).

| **`GrabbableObject`**
| Kennzeichnungskomponente für interaktionsfähige Objekte; verwaltet hierarchische Fixierung (`AttachTo`) und Freigabe (`Detach`).
|===

=== Level 3: Detailbausteine

==== `MyPalletizerController` (Klassendiagramm)

image::05_controller_l3.png[align=center, width=75%]

* **Gelenkwinkelbegrenzung (`_clamp`):** Schützt die Hardware durch hartcodierte Grenzwerte (`j1: -160..160°`, `j2: 0..90°`, `j3: -92..60°`, `j4: -360..360°`).
* **Telemetrie-Simulation (`get_angles`):** Gibt im Modus `virtual` die Werte aus `_sim_angles` und im Modus `real`/`both` die von der Hardware gelesenen Gelenkdaten zurück.

==== `World` und `WorldObject` (Klassendiagramm)

image::05_world_l3.png[align=center, width=75%]

* **Validierung (`_validate_object`):** Prüft vor dem Senden, dass Objektnamen nicht leer sind, 3D-Vektoren 3 Elemente enthalten und Skalierungswerte strikt positiv (`> 0`) sind.

==== `protocol` (Nachrichtenformate)

[cols="1,1,3", options="header"]
|===
| Nachrichtentyp (`type`) | Port | JSON-Struktur

| `"move_joints"`
| `5005`
| `{"v":1,"type":"move_joints","j1":0.0,"j2":20.0,"j3":-30.0,"j4":0.0,"speed":40}`

| `"sync_move_joints"`
| `5005`
| `{"v":1,"type":"sync_move_joints","j1":10.0,"j2":45.0,"j3":0.0,"j4":90.0,"speed":50}`

| `"led"`
| `5005`
| `{"v":1,"type":"led","r":0,"g":255,"b":0}`

| `"set_end_effector"`
| `5005`
| `{"v":1,"type":"set_end_effector","tool":"gripper"}`

| `"set_gripper_state"`
| `5005`
| `{"v":1,"type":"set_gripper_state","flag":1,"speed":50,"type_1":1}`

| `"pump"`
| `5005`
| `{"v":1,"type":"pump","enabled":true}`

| `"spawn"`
| `5006`
| `{"v":1,"type":"spawn","object_type":"box","name":"b1","position":{"x":0.1,"y":0.2,"z":0.3},"rotation":{"x":0,"y":0,"z":0},"scale":{"x":0.05,"y":0.05,"z":0.05},"color":{"r":255,"g":0,"b":0},"is_static":false}`

| `"delete"`
| `5006`
| `{"v":1,"type":"delete","name":"b1"}`

| `"clear"`
| `5006`
| `{"v":1,"type":"clear"}`
|===

==== Gelenkaktuierung in Unity

image::05_actuators_l3.png[align=center, width=70%]

* **Winkelberechnung:** Der `ArticulationJointActuator` berechnet die Geschwindigkeit via `degPerSec = max(minDegPerSec, speed * speedScale)` und führt die Rotation über `xDrive.target` im `FixedUpdate`-Zyklus nach.